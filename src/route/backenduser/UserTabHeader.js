import React from 'react';

const UserTabHeader = (props) => {
  return (
    <div style={{display: "flex", flexFlow: "column"}}>
      用户信息
      <hr/>
    </div>
  );
};

export default UserTabHeader;
